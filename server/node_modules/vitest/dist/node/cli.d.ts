import type { UserOptions } from '../types';
declare global {
    namespace NodeJS {
        interface Process {
            __vitest__: {
                options: Required<UserOptions>;
            };
        }
    }
}
